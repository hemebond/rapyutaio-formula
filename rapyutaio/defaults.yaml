# -*- coding: utf-8 -*-
# vim: ft=yaml
---
rapyutaio:
  pkg:
    name: rapyutaio
  rootgroup: root
  config: '/etc/rapyutaio'
  service:
    name: rapyutaio
  subcomponent:
    config: '/etc/rapyutaio-subcomponent-formula.conf'
  # Just here for testing
  added_in_defaults: defaults_value
  winner: defaults
